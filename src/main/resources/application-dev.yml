spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/team2_auth
    username: root
    password: team2root
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: never

  h2:
    console:
      enabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username:
    password:

server:
  port: 8011
